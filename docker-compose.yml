version: "3.9"

networks:
  purple_net:
    external: true

services:

  # ─────────────────────────────────────────
  # FRONTEND — React + Vite
  # ─────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: purple_frontend
    restart: unless-stopped
    networks:
      - purple_net

  # ─────────────────────────────────────────
  # BACKEND — FastAPI
  # ─────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: purple_backend
    restart: unless-stopped
    networks:
      - purple_net
    environment:
      - FREQTRADE_URL=http://freqtrade-huha28vvjegks7z7vt6idkxg-231205027041:8080
      - FREQTRADE_USER=${FREQTRADE_USER}
      - FREQTRADE_PASSWORD=${FREQTRADE_PASSWORD}
      - DATABASE_URL=${DATABASE_URL}
      - ENV=production